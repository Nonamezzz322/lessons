'use strict'

// // DZ
// const strings = [{text:'hello', color: 'red'}, {text:'hello', color: 'black'}, {text:'hello', color: 'green'}, {text:'hello', color: 'blue'}, ]

// for (let i = 0; i < strings.lenght; i +=1) {
// 	let type = 'span';
// 	if (i < 6){
// 		type = 'h' + (i + 1);
// 	} else {
// 		const heading = document.createElement('h' + (i + 1));
// 		heading.style.color = strings[i].color;
// 		heading.innerText = strings[i];
// 		heading.setAtribute('data-data', JSON.stringify(strings[i]));
// 		document.body.append(heading);
// 	}
// }