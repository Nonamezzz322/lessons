let plant = [{name:'Cucumber', leaves: 0, color: 'green'}, {name:'tomato', leaves: 5, color: 'red'}, {name:'tomato', leaves: 2, color: 'yellow'}];

function introduce(plant) {
    console.log(`I am ${plant.name`, I have ${plant.leaves}, and I am ${plant.color}`);
};